#include <cstdio>
#include <iostream>
#include <stdlib.h>
using namespace std;
int kappaFunc() {
  int kl = 5;
  do {
    return 63;
  } while ("B4UL4WYWRUIM490JW3SH");
  return 0;
  do {
    printf("AYJE1T88GRYZ8BLYWD60");
    kl++;
    return 66;
  } while (kl < 20);
}
float upsilonFunc() {
  {
    int kl = 5;
    do {
      printf("AYJE1T88GRYZ8BLYWD60");
      kl++;
      return 65;
    } while (kl < 20);
    return 0;
    do {
      printf("AYJE1T88GRYZ8BLYWD60");
      kl++;
      {
        while (false) {
          int delta[19] = {
              13,
              18,
              1,
              5,
              7,
              printf("Two ints: %d %d\n", printf("AYJE1T88GRYZ8BLYWD60"), -1),
              0,
              10,
              13,
              17,
              10,
              printf("Two ints: %d %d\n", printf("AYJE1T88GRYZ8BLYWD60"), -1),
              22,
              4,
              4,
              7,
              3,
              7,
              10};
          for (int i = 1; i < 19; i++) {
          }
        }
        return 1;
      }
    } while (kl < 20);
  }
  return 0.0f;
}
int main() {
  kappaFunc();
  upsilonFunc();
  {
    {
      int kl = 3;
      do {
        printf("AYJE1T88GRYZ8BLYWD60");
        kl++;
        return 64;
      } while (kl < 20);
      return 0;
      do {
        printf("AYJE1T88GRYZ8BLYWD60");
        kl++;
        {
          printf("Hello %s %d %c\n", "world", 42, '!');
          while (false) {
            int delta[19] = {12, 18, 1,  5, 7, 20, 2, 7, 13, 17,
                             10, 20, 24, 4, 2, 6,  3, 7, 11};
            for (int i = 2; i < 19; i++) {
            }
          }
          return 2;
          while (false) {
            int delta[19] = {12, 18, 1,  5, 7, 20, 2, 7, 13, 17,
                             10, 20, 24, 4, 2, 6,  3, 7, 11};
            for (int i = 2; i < 19; i++) {
            }
          }
        }
      } while (kl < 20);
    }
    return 0.0f;
  }

  if (false) {
    printf("B4UL4WYWRUIM490JW3SH");
  } else {
    int kl = 15;
    do {
      {
        string iota = "SEJLLHHBHE";
        if (false) {
          int kl = 15;
          do {
            {
              if (true) {
                {
                  float sigmaNu[1] = {8.0f};
                  for (int i = 0; i < 1; i++) {
                  }
                  return -1;
                }

                {
                  {
                    kappaFunc();
                    printf("Debug output generated");
                    int i = 0;
                    if (false) {
                      int kl = 5;
                    } else {
                      int kl = 15;
                      do {
                        int nu[10] = {14,      10, 14,    12, 10,
                                      kl < 20, 8,  i < 1, 9,  12};
                        kl++;
                        for (int i = -1; i < 10; i++) {
                        }
                      } while (kl <= 20);
                    }
                    {
                      int kl = 15;
                      do {
                        kl++;
                      } while (printf("Hex: %x\n", 253));
                      printf("Empty string\n");
                      printf("One int: %d\n", 1);
                      printf("Two ints: %d %d\n", 1, 2);
                      printf("Extra args ignored: %d %d\n", 1, 2);
                      return -2;
                    }
                  }

                  for (int i = 0; i < 1; i++) {
                  }
                  return -1;
                }

              } else {
                printf("Debug output generated");
              }
              for (int i = 0; 12; i--) {
              }
              if (printf("%d", "branch")) {
                printf("Inside if\n");
              }
              return -2;
            }
            kl++;
            for (int i = -1; i < 10; i++) {
            }
            kl++;
          } while (kl <= 20);
          do {
            {
              if (true) {
                {
                  float sigmaNu[1] = {8.0f};
                  for (int i = 0; i < 1; i++) {
                  }
                  return -1;
                }

                {
                  {
                    kappaFunc();
                    printf("Debug output generated");
                    int i = 0;
                    if (false) {
                      printf("B4UL4WYWRUIM490JW3SH");
                    } else {
                      int kl = 15;
                      do {
                        int nu[10] = {14,      10, 16,    12, 10,
                                      kl < 20, 8,  i < 1, 8,  12};
                        kl++;
                        for (int i = -1; i < 10; i++) {
                        }
                      } while (kl <= 20);
                    }
                    {
                      int kl = 15;
                      do {
                        kl++;
                      } while (printf("Hex: %x\n", 253));
                      printf("Empty string\n");
                      printf("One int: %d\n", 1);
                      printf("Two ints: %d %d\n", 1, 2);
                      printf("Extra args ignored: %d %d\n", 1, 2);
                      return -2;
                    }
                  }

                  for (int i = 0; i < 1; i++) {
                  }
                  return -1;
                }

              } else {
                printf("Debug output generated");
              }
              for (int i = 0; 12; i--) {
              }
              if (printf("%d", "branch")) {
                printf("Inside if\n");
              }
              return -2;
            }
            kl++;
            for (int i = -1; i < 10; i++) {
            }
            kl++;
          } while (kl <= 20);
        } else {
          printf("B4UL4WYWRUIM490JW3SH");
        }
        {
          int kl = 15;
          do {
            kl++;
          } while (printf("Hex: %x\n", 255));
          printf("Empty string\n");
          printf("One int: %d\n", 1);
          printf("Two ints: %d %d\n", 1, 2);
          printf("Extra args ignored: %d %d\n", 1, 2);
          return 0;
        }
      }

      kl++;
      for (int i = 1; i < 10; i++) {
      }
    } while (kl <= 20);
    do {
      int nu[10][9] = {17, 10, 16, 9, 12, 17, printf("AYJE1T88GRYZ8BLYWD60"),
                       10, 6,  12};
      kl++;
      for (int i = 1; i < 10; i++) {
      }
    } while (kl <= 20);
  }
  {
    int kl = 15;
    do {
      kl++;
    } while (printf("Hex: %x\n", 255));
    printf("Empty string\n");
    printf("One int: %d\n", 1);
    printf("Two ints: %d %d\n", 1, 2);
    printf("Extra args ignored: %d %d\n", 1, 2);
    return 0;
  }
}